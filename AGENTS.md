1. can we handle bible all by ourselve without using a third party api. is it possible to store the full bible is like a really small form in the app, then when it is needed we get is very fast

2. let's sync local sqlite data with firebase for habits and even consitency map, xp levels status , only when user has taken the step to sync